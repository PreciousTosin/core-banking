apiVersion: v1
kind: Service
metadata:
  name: redpanda
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: 9092
      targetPort: 9092
    - port: 9644
      targetPort: 9644
  selector:
    app: redpanda
