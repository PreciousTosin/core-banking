# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Local isolated implementation worktrees.
/.worktrees/
/.claude/worktrees/

# Maven build output for the funds-core module.
/services/funds-core/target/

# Mermaid tooling is installed only in an invocation-owned temporary root.
/architecture/tooling/node_modules/
/architecture/diagrams/generated/
